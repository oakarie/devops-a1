name: Deploy Backend

on:
  # Manual only – real deployment is handled by Render via render.yaml
  workflow_dispatch:

jobs:
  verify-production-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Check Render backend health endpoint
        run: |
          curl -f https://gpt-findability-backend.onrender.com/healthimage.png